/* Translation is a presentation layer: never change stored photo/site records. */
(() => {
  const translations = {
    "此页面不存在": "This page could not be found",
    "链接可能已失效，或页面已被移动。照片仍在作品集中等你。": "The link may be out of date, or the page may have moved. You can still explore the photographs in the gallery.",
    "页面导航": "Page navigation",
    "Hugo.aviation | 航空摄影 Blog": "Hugo.aviation | Aviation Photography Blog",
    "全部航空作品 | Hugo.aviation": "All Aviation Photos | Hugo.aviation",
    "Hugo.aviation 的个人航空摄影作品，记录客机、机场运行、进近起降与不同光线下的飞行瞬间。": "A personal aviation photography portfolio by Hugo.aviation, capturing airliners, airport operations, approaches and departures, and moments of flight in changing light.",
    "记录客机、机场运行、进近起降与不同光线下的飞行瞬间。": "Airliners, airport operations, approaches and departures, and moments of flight in changing light.",
    "浏览 Hugo.aviation 的全部航空摄影作品，并按机型、航空公司、机场、飞行阶段和时间筛选。": "Explore all aviation photographs by Hugo.aviation. Filter by aircraft, airline, airport, phase of flight and time of day.",
    "按机型、航空公司、机场、飞行阶段和时间浏览航空摄影作品。": "Browse aviation photographs by aircraft, airline, airport, phase of flight and time of day.",
    "你好": "Hello",
    "欢迎来到 Hugo.aviation": "Welcome to Hugo.aviation",
    "查看航空作品": "Explore the photographs",
    "浏览航迹故事": "Explore the stories",
    "作品": "Photos", "航迹": "Stories", "手记": "Journal", "地图": "Map", "关于": "About", "联系": "Contact",
    "返回 Hugo.aviation 首页": "Return to the Hugo.aviation homepage", "主导航": "Main navigation",
    "挥手": "Waving hand", "摄影数据概览": "Photography at a glance",
    "酷航黄色客机机头特写": "Close-up of the nose of a yellow Scoot airliner",
    "精选航空摄影作品": "Selected aviation photographs",
    "卡塔尔埃米尔空军重型运输机进近": "Qatar Emiri Air Force heavy transport aircraft on approach",
    "重型运输机的最后进近": "A heavy transport on final approach",
    "卡塔尔埃米尔空军运输机放下起落架，四台发动机与宽厚机翼在暗色天空下完整展开。": "With its landing gear down, a Qatar Emiri Air Force transport reveals all four engines and its broad wings against a dark sky.",
    "黑鸟掠过机库中轴": "The Blackbird along the hangar’s centreline",
    "从 SR-71 机身上方望向机首，双发短舱、双垂尾与机库结构在黑白画面中收束成锐利的速度感。": "Looking forward over the SR-71’s fuselage, the twin engine nacelles, twin tails and hangar structure converge in black and white, creating a sharp sense of speed.",
    "羽田 T3 的观景边界": "At the edge of Haneda’s Terminal 3 observation deck",
    "橙色机身穿过蓝天": "An orange fuselage against a blue sky",
    "廊桥旁的黑白 787": "A 787 at the jet bridge, in black and white",
    "廊桥旁的 787": "A 787 at the jet bridge",
    "查看全部作品": "View all photographs",
    "航空摄影航迹故事": "Stories behind the photographs",
    "从观景台读懂机坪": "Reading the apron from the observation deck",
    "航站楼、滑行线、廊桥和地勤车辆不断改变画面；飞机停靠的位置，也在讲述机场的运行节奏。": "Terminals, taxiway markings, jet bridges and ground vehicles continually reshape the scene. Where each aircraft parks tells its own story of the airport’s rhythm.",
    "等飞机，也等画面归位": "Waiting for the aircraft—and the composition",
    "从推出到滑行，每一步都只有短暂窗口。我会等机身、前景和跑道线条落在最清楚的位置。": "From pushback to taxi, every stage offers only a brief window. I wait for the fuselage, foreground and runway lines to align in the clearest composition.",
    "把机场的秩序留在照片里": "Preserving the order of an airport in a photograph",
    "塔台、机位与远处起降的飞机共同构成层次，让照片不只记录某一架飞机，也保留当时的现场。": "The tower, aircraft stands and distant arrivals and departures build layers into the frame, capturing not just one aircraft but the scene around it.",
    "机场边的拍摄手记": "Notes from the airport perimeter",
    "从晴空进近、夜间滑行到繁忙货运机坪，用三组画面记录不同光线下的机身姿态与机场秩序。": "Three sets of images—from clear-sky approaches and night taxiing to a busy cargo apron—capture aircraft profiles and airport operations in different light.",
    "雪绒花 A350 划过晴空": "An Edelweiss A350 across a clear sky",
    "雪绒花 A340 划过晴空": "An Edelweiss A340 across a clear sky",
    "起落架放下后，细长机翼、两台发动机和红色机首在蓝天里一目了然；干净侧面让涂装成为画面焦点。": "With the landing gear down, the slender wings, two engines and red nose stand out against the blue sky. The clean side-on view makes the livery the focus of the frame.",
    "起落架放下后，细长机翼、四台发动机和红色机首在蓝天里一目了然；干净侧面让涂装成为画面焦点。": "With the landing gear down, the slender wings, four engines and red nose stand out against the blue sky. The clean side-on view makes the livery the focus of the frame.",
    "华航彩绘机穿过夜色": "A China Airlines special livery in the night",
    "滑行灯、航灯与跑道反光围住机身，彩绘在低照度中仍保留清楚层次。": "Taxi lights, navigation lights and reflections from the runway surround the fuselage, while the special livery retains its definition in the low light.",
    "两架 747 的货运机坪": "Two 747s on the cargo apron",
    "日本货物航空与 Air ACT 的 747 前后错落，滑行线、机位和不同距离把繁忙机坪整理出清楚层次。": "The Nippon Cargo Airlines and Air ACT 747s sit at different depths in the frame. Taxiway markings, aircraft stands and distance bring clear layers to a busy apron.",
    "镜头抵达过的机场": "Airports through my lens",
    "正在整理拍摄城市": "Loading photographed cities",
    "正在载入城市坐标": "Loading city coordinates",
    "航空摄影机场地图": "Map of aviation photography locations",
    "航空摄影机场地图封面": "Cover photograph for the airport map",
    "选择拍摄机场": "Select an airport", "显示全部拍摄机场": "Show all photographed airports",
    "机场地图封面": "Airport map cover photograph", "机场代表作品": "Featured airport photograph",
    "机场拍摄记录": "Airport photography log", "等待作品数据": "Waiting for photographs",
    "机场目录暂时不可用": "The airport directory is temporarily unavailable",
    "底图暂时不可用": "The base map is temporarily unavailable", "地图暂时不可用": "The map is temporarily unavailable",
    "你好，我是 Hugo.aviation。": "Hello, I’m Hugo.aviation.",
    "我长期拍摄民航客机、机场运行与飞行途中的天空景观。这个 blog 会持续整理我喜欢的飞机、光线和机位。": "I photograph airliners, airport operations and views of the sky in flight. This blog is an ongoing collection of the aircraft, light and spotting locations I enjoy.",
    "收录航空作品": "Photographs", "拍摄机场": "Airports photographed", "记录机型": "Aircraft types",
    "演示作品": "Demo photographs", "真实机场": "Real airports", "真实机型": "Real aircraft types",
    "联系 Hugo.aviation": "Contact Hugo.aviation", "电子邮件": "Email", "抖音": "Douyin",
    "作品页导航": "Gallery navigation", "返回首页": "Return home", "作品分类": "Photo categories",
    "全部": "All", "时间": "Time", "航司": "Airline", "机型": "Aircraft", "机场": "Airport", "阶段": "Phase",
    "搜索作品": "Search photographs", "作品分类选项": "Photo filter options",
    "多选": "Multi-select", "清除此项": "Clear this filter", "重置全部": "Reset all",
    "搜索航空作品": "Search aviation photographs", "搜索机型、航司、机场或文件名": "Search aircraft, airline, airport or filename",
    "清除搜索": "Clear search", "搜索": "Search", "全部航空摄影作品": "All aviation photographs",
    "暂无匹配作品": "No matching photographs", "清除筛选": "Clear filters",
    "照片预览": "Photo preview", "关闭预览": "Close preview", "上一张": "Previous photograph", "下一张": "Next photograph", "照片信息": "Photo information",
    "地面停场": "Parked", "滑行": "Taxiing", "起飞": "Takeoff", "进近 / 降落": "Approach / landing",
    "空中 / 巡航": "Airborne / cruise", "巡航": "Cruise", "其他": "Other", "日间": "Daytime", "夜间": "Night",
    "拍摄时间": "Time of day", "航空公司": "Airline", "飞行阶段": "Phase of flight",
    "中国": "China", "台湾": "Taiwan", "日本": "Japan", "加拿大": "Canada", "美国": "United States", "阿联酋": "United Arab Emirates",
    "广州": "Guangzhou", "香港": "Hong Kong", "澳门": "Macau", "东京": "Tokyo", "上海": "Shanghai", "台北": "Taipei", "温哥华": "Vancouver", "迪拜": "Dubai", "西雅图": "Seattle",
    "桃园": "Taoyuan", "都柏林": "Dublin", "伦敦": "London", "爱尔兰": "Ireland", "英国": "United Kingdom",
  };
  let translationKeys = Object.keys(translations).sort((a, b) => b.length - a.length);
  const languageKey = "hugo.language";
  const normalize = value => /^zh(?:-|$)/i.test(value || "") ? "zh-CN" : /^en(?:-|$)/i.test(value || "") ? "en" : null;
  let saved;
  try { saved = localStorage.getItem(languageKey); } catch { /* Storage may be disabled. */ }
  const requested = new URL(location.href).searchParams.get("lang");
  let language = normalize(requested) || normalize(saved)
    || (navigator.languages || [navigator.language]).map(normalize).find(Boolean) || "en";
  const adminMode = document.documentElement.hasAttribute("data-content-editor");
  if (!adminMode) document.documentElement.lang = language;
  let editorial = new Map();
  const localized = (source, locale) => editorial.get(source.trim())?.[locale];

  function english(value) {
    const text = String(value ?? "");
    const trimmed = text.trim();
    const custom = localized(text, "en");
    if (typeof custom === "string") return text.replace(trimmed, () => custom);
    if (Object.hasOwn(translations, trimmed)) return text.replace(trimmed, translations[trimmed]);
    // Structured labels retain all names, codes, numbers and camera parameters.
    let result = text
      .replace(/(\d+) 个拍摄城市/g, "$1 photographed cities")
      .replace(/(\d+) 张已定位作品/g, "$1 geolocated photographs")
      .replace(/(\d+) 张作品/g, (_, n) => `${n} photograph${n === "1" ? "" : "s"}`)
      .replace(/(\d+) 张/g, (_, n) => `${n} photo${n === "1" ? "" : "s"}`)
      .replace(/查看 ([A-Z0-9]+) 作品/g, "View $1 photographs")
      .replace(/打开照片：/g, "Open photograph: ")
      .replace(/(.+?) 于 (.+?) 的航空摄影作品/g, "$1 at $2 — aviation photograph")
      .replace(/航空摄影作品/g, "Aviation photograph")
      .replace(/机位 \/ 参数 /g, "Location / settings: ")
      .replace(/备注 /g, "Notes: ")
      .replace(/编辑/g, "Edit").replace(/未标题/g, "Untitled");
    // Longest first prevents a short UI label from splitting a full phrase.
    for (const key of translationKeys) {
      if (result.includes(key)) result = result.replaceAll(key, translations[key]);
    }
    return result.replace(/清除(.+)/g, "Clear $1").replace(/选项/g, " options").replace(/筛选/g, " filter").replace(/，/g, ", ");
  }

  if (adminMode) {
    window.HugoTranslationDefaults = { english, entries: { ...translations } };
    return;
  }
  const originals = new WeakMap();
  const attributes = ["alt", "title", "aria-label", "placeholder"];
  function localizeLink(link) {
    if (!link.matches('a[href]')) return;
    const href = link.getAttribute("href");
    if (!href || href.startsWith("#")) return;
    let url;
    try { url = new URL(href, location.href); } catch { return; }
    if (url.origin !== location.origin || !["/", "/works"].includes(url.pathname)) return;
    url.searchParams.set("lang", language === "en" ? "en" : "zh");
    const localized = `${url.pathname}${url.search}${url.hash}`;
    if (href !== localized) link.setAttribute("href", localized);
  }
  function update(node, key, read, write) {
    const current = read();
    if (current === null) return;
    let record = originals.get(node);
    if (!record) originals.set(node, record = new Map());
    const previous = record.get(key);
    const source = previous && current === previous.output ? previous.source : current;
    const zh = localized(source, "zh");
    const output = language === "en" ? english(source)
      : (typeof zh === "string" ? source.replace(source.trim(), () => zh) : source);
    record.set(key, { source, output });
    if (current !== output) write(output);
  }
  function translate(root) {
    if (root.nodeType === 3) {
      if (!root.parentElement?.closest("script,style,noscript,textarea,[data-no-translate]")) {
        update(root, "text", () => root.nodeValue, value => { root.nodeValue = value; });
      }
      return;
    }
    if (root.nodeType !== 1 || root.closest("script,style,noscript,textarea,[data-no-translate]")) return;
    localizeLink(root);
    for (const name of attributes) {
      if (root.hasAttribute(name)) update(root, name, () => root.getAttribute(name), value => root.setAttribute(name, value));
    }
    if (root.matches('meta[name="description"],meta[property$=":title"],meta[property$=":description"],meta[name^="twitter:"]')) {
      update(root, "content", () => root.getAttribute("content"), value => root.setAttribute("content", value));
    }
    for (const child of root.childNodes) translate(child);
  }
  const observer = new MutationObserver(records => {
    observer.disconnect();
    const roots = new Set();
    for (const record of records) {
      if (record.type === "childList") record.addedNodes.forEach(node => roots.add(node));
      else roots.add(record.target);
    }
    roots.forEach(translate);
    observe();
  });
  function observe() {
    observer.observe(document.documentElement, { subtree: true, childList: true, characterData: true, attributes: true, attributeFilter: [...attributes, "content", "href"] });
  }
  function refresh() {
    observer.disconnect();
    translate(document.documentElement);
    document.querySelector('meta[property="og:locale"]')?.setAttribute("content", language === "en" ? "en_US" : "zh_CN");
    document.querySelectorAll("[data-language]").forEach(button => button.setAttribute("aria-pressed", String(button.dataset.language === language)));
    observe();
  }
  function setLanguage(value) {
    const next = normalize(value);
    if (!next) return;
    language = next;
    try { localStorage.setItem(languageKey, language); } catch { /* Still switch for this page. */ }
    const url = new URL(location.href);
    url.searchParams.set("lang", language === "en" ? "en" : "zh");
    history.replaceState(null, "", `${url.pathname}${url.search}${url.hash}`);
    document.documentElement.lang = language;
    refresh();
    window.dispatchEvent(new CustomEvent("languagechange"));
  }
  window.HugoI18n = {
    get language() { return language; }, english, setLanguage,
    setLocalization(value) {
      editorial = new Map(Object.values(value?.entries || {})
        .filter(entry => entry && typeof entry.source === "string")
        .map(entry => [entry.source, entry]));
      refresh();
    },
    addTranslations(entries) {
      Object.assign(translations, entries);
      translationKeys = Object.keys(translations).sort((a, b) => b.length - a.length);
      refresh();
    },
  };
  // Back/forward can restore a URL with a different language without reloading.
  window.addEventListener("popstate", () => {
    const next = normalize(new URL(location.href).searchParams.get("lang"));
    if (!next || next === language) return;
    language = next;
    document.documentElement.lang = language;
    refresh();
    window.dispatchEvent(new CustomEvent("languagechange"));
  });
  observe();
  document.addEventListener("DOMContentLoaded", () => {
    document.querySelectorAll("[data-language]").forEach(button => button.addEventListener("click", () => setLanguage(button.dataset.language)));
    refresh();
    // Read only the compact editorial dictionary, never the admin secret.
    if (typeof fetch === "function") fetch("/api/translations", {
      cache: "no-cache", signal: globalThis.AbortSignal?.timeout?.(10000),
    }).then(response => response.ok ? response.json() : null)
      .then(value => { if (value) window.HugoI18n.setLocalization(value); })
      .catch(() => { /* Built-in translations stay usable during an outage. */ });
  }, { once: true });
})();
